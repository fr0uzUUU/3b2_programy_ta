#include <iostream>
#include <time.h>
#include <algorithm>
#include <stdlib.h>
using namespace std;

int main()
{
    int tab[5], n = 5;
	srand(time(NULL));
	for (int i = 0; i < n; i++)
	{
		tab[i] = rand() % n;
		cout << tab[i] << " ";
	}
	
}